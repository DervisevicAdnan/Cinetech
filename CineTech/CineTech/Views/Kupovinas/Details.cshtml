@model CineTech.Models.Kupovina

@{
    ViewData["Title"] = "Details";
}

<h1 class="text-center">Details</h1>

<div class="container">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <h4>Kupovina</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">Cijena</dt>
                <dd class="col-sm-10">@Html.DisplayFor(model => model.cijena)</dd>
                <dt class="col-sm-2">Datum</dt>
                <dd class="col-sm-10">@Html.DisplayFor(model => model.datum)</dd>
                <dt class="col-sm-2">Vrijeme</dt>
                <dd class="col-sm-10">@Html.DisplayFor(model => model.vrijeme)</dd>
                <dt class="col-sm-2">KorisnikId</dt>
                <dd class="col-sm-10">@Html.DisplayFor(model => model.KorisnikId)</dd>
            </dl>
        </div>
    </div>
</div>

<div class="text-center mt-3">
    <a asp-action="Edit" asp-route-id="@Model?.id" class="btn btn-primary">Edit</a> |
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>
