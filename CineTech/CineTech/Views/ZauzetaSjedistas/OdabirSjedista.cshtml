@model IEnumerable<CineTech.Models.ZauzetaSjedista>

@{
    ViewData["Title"] = "OdabirSjedista";
}
<h1>Create</h1>

<h4>Zauzeta Sjedista</h4>
<hr />
<!--
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="red" class="control-label"></label>
                <select asp-for="red" class="form-control">
                    <option value="">-- Odaberite Red --</option>
                    foreach (var sjediste in Model.SlobodnaSjedista.Select(s => s.Red).Distinct())
                    {
                        <option value="sjediste">sjediste</option>
                    }
                </select>
                <span asp-validation-for="Red" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Mjesto" class="control-label"></label>
                <select asp-for="Mjesto" class="form-control">
                    <option value="">-- Odaberite Mjesto --</option>
                    foreach (var sjediste in Model.SlobodnaSjedista.Where(s => s.Red == Model.Red))
                    {
                        <option value="sjediste.Mjesto">sjediste.Mjesto</option>
                    }
                </select>
                <span asp-validation-for="Mjesto" class="text-danger"></span>
            </div>

            <input type="hidden" asp-for="ProjekcijaId" value="Model.ProjekcijaId" />

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
-->

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
